rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // PERICOLOSO IN PRODUZIONE: Permette a chiunque di leggere e scrivere
      allow read, write: if true;
    }
  }
}
